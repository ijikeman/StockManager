# Set root logging to INFO so request logging appears in stdout
logging.level.root=INFO

# Optional: tune specific packages
logging.level.org.springframework.web.filter.CommonsRequestLoggingFilter=INFO
